apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "np-sync.fullname" . }}-test-connection"
  labels:
    {{- include "np-sync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "np-sync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
